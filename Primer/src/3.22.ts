let condition = true;
let person = 'Bob';
const age = 40;

// TypeScript compiler infers the type of each variable or constant from the value
console.log(typeof(condition));
console.log(typeof(person));
console.log(typeof(age));